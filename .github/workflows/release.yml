name: Release

# tag 即版本。三道门:
#   ① tag 必须与 package.json 的 version 一致;
#   ② 发布前所有自检必须绿;
#   ③ 发布**之后**核对:registry 上那一份与本 commit 构建的逐文件一致。
#
# 发布身份用 npm 的 **Trusted Publishing**(OIDC:没有长期令牌,也不需要 OTP):
#   · 在 npmjs.com 的包设置里把它配成该包的 trusted publisher
#     (Repository: Clearailhc/clearai-dsh · Workflow filename: release.yml · Environment 留空);
#   · 本文件声明 `id-token: write`(见下面 permissions);
#   · **runner 上的 npm 必须 ≥ 11.5.1** —— OIDC 交换是 npm CLI 自己做的,旧 npm 不认识它,
#     只会甩一句 ENEEDAUTH。Node 22 自带 npm 10.9.8,所以这里跑 Node 24;下面还有一道明写的门。
#   · 用 OIDC 时**不要**给 setup-node 传 `registry-url`:它往 .npmrc 写
#     `_authToken=${NODE_AUTH_TOKEN}`,而这条路下那个变量不存在 —— 那是另一条会让人查半天的错。
#     包没有 scope、发的是默认 registry,本来就不需要那行。
#
# 2026-09-15 的教训:这条发布路径**连续三版都没有真的执行过** ——
# 0.1.0/0.1.1/0.1.2 都是人**手工先发**,于是幂等分支每次 `exit 0`、workflow 报绿。
# 等到 v0.1.3 第一次真去发,2 秒被拒(没有身份)。所以现在:不管这一版是发出去的还是跳过的,
# 最后都必须被核对成「registry 上确实有这一份,而且内容就是本 commit 构建的」——
# 门的绿要自己挣来,不能靠一句 `exit 0`。
on:
  push:
    tags: ['v*']

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      # `contents: write` 是给最后那一步建 GitHub Release 用的(它要写 releases)。
      # **发布本身不需要它** —— npm 那边的身份走 OIDC(下面那个 id-token)。
      contents: write
      id-token: write # Trusted Publishing 要它
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          # Node 24 自带 npm ≥ 11.5.1;trusted publishing 的硬要求就是它。
          node-version: '24'
      - name: Install dependencies
        # 与 CI 同一条:`npm test` 的第一步与打包自检都要 esbuild(devDependency),
        # 不装依赖会在 Suites 这一步直接 ERR_MODULE_NOT_FOUND。
        run: npm ci
      - name: npm 必须够新(OIDC trusted publishing 的最低要求)
        # 明写这道门:达不到就**现在**红,而不是等到最后甩一句 ENEEDAUTH 让人回查半天。
        run: |
          echo "node $(node --version) · npm $(npm --version)"
          node -e '
            const want = [11, 5, 1]
            const got = process.argv[1].split(".").map(Number)
            const ok = got[0] > want[0] || (got[0] === want[0] && (got[1] > want[1] || (got[1] === want[1] && got[2] >= want[2])))
            if (!ok) {
              console.error(`::error::npm ${process.argv[1]} 太旧 —— trusted publishing 需要 ≥ 11.5.1(用更新的 node,或 npm install -g npm@latest)`)
              process.exit(1)
            }
          ' "$(npm --version)"
      - name: Enable pnpm
        run: |
          corepack enable
          pnpm --version
      - name: Tag must match the manifest
        run: |
          tag="${GITHUB_REF_NAME#v}"
          manifest="$(node -p "require('./package.json').version")"
          echo "tag=$tag manifest=$manifest"
          [ "$tag" = "$manifest" ] || { echo "::error::tag $tag does not match package.json $manifest"; exit 1; }
      - name: Suites
        run: npm test
      - name: Build and self-check
        run: |
          node tools/build-package.mjs
          node tools/verify-package.mjs
      - name: Install the DSH CLI
        run: npm install --no-save --prefix "$RUNNER_TEMP/dsh-cli" @deepseek-ai/dsh
      - name: Clean install (mechanical half)
        env:
          DSH_CLI_PREFIX: ${{ runner.temp }}/dsh-cli
          DSH_CHECKOUT: ${{ github.workspace }}
        run: node tools/verify-clean-install.mjs
      - name: OIDC claims(与 npm 上那份配置逐字对比用)
        # 交换被 registry 拒时,npm 只会说一句 `OIDC token exchange error - package not found` ——
        # 而真实原因几乎总是「OIDC 声明与 npmjs.com 上那份 trusted publisher 对不上」。
        # 这里把声明原文打出来,于是"哪个字段不一致"是**看出来的**,不是猜出来的。
        # (只打印声明字段,不打印令牌本身;这些都是公开信息:仓库名、workflow 路径、ref。)
        run: |
          token="$(curl -sS -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
            "$ACTIONS_ID_TOKEN_REQUEST_URL&audience=npm:registry.npmjs.org" \
            | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{try{process.stdout.write(JSON.parse(s).value||"")}catch{process.stdout.write("")}})')"
          if [ -z "$token" ]; then
            echo "::warning::拿不到 id_token(OIDC 环境不可用)"
            exit 0
          fi
          echo "$token" | node -e '
            let s = ""
            process.stdin.on("data", (d) => (s += d)).on("end", () => {
              const j = JSON.parse(Buffer.from(s.trim().split(".")[1], "base64url").toString())
              console.log("npm 会拿这些声明去换令牌 —— 必须与 npmjs.com 上那份配置逐字一致:")
              for (const k of ["repository", "repository_owner", "repository_owner_id", "workflow_ref", "job_workflow_ref", "environment", "ref", "event_name"]) {
                console.log(`  ${k.padEnd(20)} ${j[k] === undefined ? "(没有这个声明)" : j[k]}`)
              }
            })
          '

      - name: Publish the built package
        # 从 dist/ 发布:仓库根的 package.json 永不发布。
        # **幂等**:0.1.0 是手工首发(2FA 只能人敲),之后再打同一个 tag 不能让 CI 红着报
        # "version already published" —— 先问 registry 有没有这个版本,有就跳过。
        # 但"跳过"不再等于"没发布成功":下面那道核对会把两种情形都验成同一个事实。
        # `--provenance` 在 trusted publishing 下已是默认行为,留着是为了「退回 granular token」那条路也对。
        # 退回令牌方式时:给 setup-node 加回 registry-url,并加 NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}。
        #
        # 为什么要把 OIDC 环境与 debug 日志打出来:2026-09-15 实测,npm 的 `oidc()` 在拿不到
        # id_token 时**静默返回**,然后一句 `ENEEDAUTH` 顶上来 —— 到底是"job 没有 id-token 权限"
        # 还是"拿 token 去换的时候被 registry 拒了",从输出里**完全看不出来**。
        # 这里把两件事都摆到台面上:环境在不在(不打印值),以及失败时 npm 的 debug 日志尾巴。
        run: |
          version="$(node -p "require('./package.json').version")"
          if [ -n "$ACTIONS_ID_TOKEN_REQUEST_URL" ] && [ -n "$ACTIONS_ID_TOKEN_REQUEST_TOKEN" ]; then
            echo "OIDC 环境:在(GitHub 已注入 id-token 请求参数)"
          else
            echo "::warning::OIDC 环境:不在 —— 这个 job 没有拿到 id-token: write,trusted publishing 走不了"
          fi
          if npm view "clearai-dsh@$version" version >/dev/null 2>&1; then
            echo "::notice::clearai-dsh@$version 已经在 registry 上 —— 本次没有发布任何东西(首发是手工完成的,幂等跳过)"
          else
            echo "registry 上还没有 $version → 发布 dist/clearai-dsh"
            if ! npm publish ./dist/clearai-dsh --access public --provenance; then
              echo "::group::npm debug 日志尾巴(OIDC 交换失败的原因在这里)"
              latest="$(ls -t "$HOME/.npm/_logs/"*debug-0.log 2>/dev/null | head -1)"
              if [ -n "$latest" ]; then tail -70 "$latest"; else echo "(没有 debug 日志)"; fi
              echo "::endgroup::"
              exit 1
            fi
          fi
      - name: Registry must carry exactly this source
        # 「跳过」那条路的解药,也是发布本身的自检:不管上面是**发了**还是**跳了**,
        # 都要求 registry 上那一份与本 commit 构建的**逐文件一致**。
        # 没有它,一次静默的失败、或一次用手工发的**内容不同**的版本,都能让 tag 报绿。
        # 这条比对在 2026-09-15 用已发布的 0.1.2 实测过:与 v0.1.2 那棵树逐文件相同。
        #
        # 重试窗口**必须够长**:npm 接受发布之后会明说「Your package is being processed and may
        # take a few minutes to become available」,而 packument 在那几分钟里查不到这个版本。
        # 0.2.2 就撞上了:发布本身成功(`+ clearai-dsh@0.2.2`,provenance 也落了透明度日志),
        # 但这道闸只有 5×5 秒,窗口用完就红,连带后面的 GitHub Release 一起跳过。
        # 现在给 6 分钟(18 × 20 秒)——闸门该等的是**事实**,不是自己的耐心。
        run: |
          version="$(node -p "require('./package.json').version")"
          ok=0
          for attempt in $(seq 1 18); do
            url="$(npm view "clearai-dsh@$version" dist.tarball 2>/dev/null || true)"
            if [ -n "$url" ] && curl -fsSL "$url" -o "$RUNNER_TEMP/published.tgz"; then ok=1; break; fi
            echo "registry 上还拿不到 $version 的 tarball,20 秒后重试($attempt/18)"
            sleep 20
          done
          [ "$ok" = 1 ] || { echo "::error::registry 上没有 $version 的可下载 tarball —— 这一步没有发布成功"; exit 1; }
          mkdir -p "$RUNNER_TEMP/published"
          tar -xzf "$RUNNER_TEMP/published.tgz" -C "$RUNNER_TEMP/published"
          # INVENTORY.txt 是**构建自己的**文件清单,不在清单的 files 白名单里,发行物里本来就没有它。
          diff -r "$RUNNER_TEMP/published/package" dist/clearai-dsh --exclude=INVENTORY.txt
          echo "registry 上的 $version 与本 commit 构建的逐文件一致 ✓"
      - name: Create the GitHub Release
        # tag 已经等于「registry 上那一份源码」(上一步刚核对过),这里补的是**人看的那一面**。
        #
        # 说明**从 CHANGELOG 里抽该版本那一段**(唯一账本),不用 `--generate-notes` 拿 commit
        # 列表自己编一份 —— 那是第二本账,而且会随 commit 数漂移。人工回填也用同一个抽取器,
        # 所以两边印出来的话永远一致。
        #
        # 幂等:已存在就改写说明,重跑同一个 tag 不会红。
        # 这一步是 2026-09-16 补的:此前 Releases 面板停在手建的 0.1.2,而 npm 已经到 0.1.5 ——
        # 三个对象(tag / npm / GitHub Release)本来就是三件事,缺哪一步哪一步就不动。
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          version="$(node -p "require('./package.json').version")"
          node tools/changelog-section.mjs "$version" > "$RUNNER_TEMP/release-notes.md"
          # 标题里那句短语也来自同一段:段首那段黑体(没有就只写版本号)。
          title="$(node tools/changelog-section.mjs "$version" --title)"
          if gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1; then
            # 已存在就**只改说明,不动标题** —— 标题可能是人写过的(手工那三个就带一句短语),
            # 重跑同一个 tag 不该把它抹平。
            echo "已存在 → 改写说明:$GITHUB_REF_NAME"
            gh release edit "$GITHUB_REF_NAME" --notes-file "$RUNNER_TEMP/release-notes.md"
          else
            echo "新建 Release:$GITHUB_REF_NAME → $title"
            gh release create "$GITHUB_REF_NAME" --title "$title" --notes-file "$RUNNER_TEMP/release-notes.md"
          fi
