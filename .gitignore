# 依赖与产物
node_modules/
dist/

# 编辑器与系统
.DS_Store
*.log

# 运行产物:工作区骨架与账本证据(内核自己写,不是源码)
clear/

# 长测归档:判读证据入库(trajectory.txt / summary.json / meta.json / index.json),
# 重型现场留在盘上不入库(工作区现场、原始会话日志、stdout 快照)——`*.log` 已经在上面忽略。
# 为什么这样分:轨迹解码后小且可 grep,是"模型到底做了什么"的可读记录;
# 工作区与会话原件可能很大,留在归档目录里供本地复算(tools/e2e-replay.mjs)。
docs/optimization/e2e-logs/*/*/workspace/
docs/optimization/e2e-logs/*/*/session.jsonl.zstd
